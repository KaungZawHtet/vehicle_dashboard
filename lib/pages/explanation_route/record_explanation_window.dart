

import 'package:flutter/material.dart';

class RecordExplanationWindow extends StatelessWidget {
  const RecordExplanationWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Record"));
  }
}