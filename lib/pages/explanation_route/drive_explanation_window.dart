import 'package:flutter/material.dart';

class DriveExplanationWindow extends StatelessWidget {
  const DriveExplanationWindow({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Drive"));
  }
}